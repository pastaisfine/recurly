# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v54.0.0/ before writing any code.

# Skills are opencode skills

The project skills in `.opencode/skills/` are for opencode, not Claude Code. When a skill is triggered, opencode handles it — never delegate skill handling to Claude Code.
